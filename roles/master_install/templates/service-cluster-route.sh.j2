#!/bin/bash
sudo ip route replace {{ service_cluster_ip_range }} \
{% for node_hostname in nodes %}
    nexthop via {{ lookup('pipe', 'getent hosts ' + node_hostname | quote).split() | first }} weight 1 \
{% endfor %}
